# Fabric runtime assets

This directory holds the Fabric connection profile and wallet used by the
Express backend to talk to the local Fabric test-network.

- `connection/` — connection profiles (generated by `fabric-network/network.sh enroll`)
- `wallet/` — file-system wallet managed by `fabric-network`

Both subfolders are `.gitignore`d. Do not commit credentials.
